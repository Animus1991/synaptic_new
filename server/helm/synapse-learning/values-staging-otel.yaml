# Staging overrides — enable OpenTelemetry export (B2)
# helm upgrade … -f values.yaml -f values-staging-otel.yaml

otel:
  enabled: true
  serviceName: synapse-learning-api-staging
  exporterOtlpEndpoint: "http://otel-collector.observability:4318"

replicaCount: 2
