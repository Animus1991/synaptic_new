/** Wave C2 — first feature folder: client RUM (web-vitals → POST /v1/rum). */
export {
  buildRumPayload,
  initWebVitalsRum,
  rumEndpoint,
  setRumRouteGetter,
  type RumMetricName,
  type RumPayload,
} from './webVitalsRum';
