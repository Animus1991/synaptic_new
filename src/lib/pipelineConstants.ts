/** Semantic version of the client content-recognition pipeline (worker + analysis). */
export const CONTENT_PIPELINE_VERSION = '2.5.1';

/** PDF cover thumbnail render pipeline (Sprint L17). */
export const PDF_THUMBNAIL_PIPELINE_VERSION = '1.0.0';
