/** Capacitor companion app config — run `npm run build && npx cap sync` after installing @capacitor/cli. */
const config = {
  appId: 'com.synapse.learning',
  appName: 'Synapse',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
